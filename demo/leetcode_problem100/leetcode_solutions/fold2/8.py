class Solution: def myAtoi(self, s: str) -> int:     # Step 1: Remove leading whitespace     s = s.strip()     if not s:         return 0     # Step 2: Check for sign     sign = 1     if s[0] == '-':         sign = -1         s = s[1:]     elif s[0] == '+':         s = s[1:]     # Step 3: Read digits until non-digit character or end of string     digits = ''     for char in s:         if char.isdigit():             digits += char         else:             break     # Step 4: Convert digits to integer     if not digits:         return 0     result = int(digits) * sign     # Step 5: Clamp result to 32-bit signed integer range     result = max(min(result, 2**31 - 1), -2**31)     return result